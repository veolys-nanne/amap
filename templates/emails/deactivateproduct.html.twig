{% extends 'emails/email.html.twig' %}

{% block body %}
    <p>
        Une demande a été émise pour la désactivation du produit {{ product.name }}.
    </p>
    <p>Veuillez vous connecter sur le <a href="{{ url('product_form', {role : role, id: product.id}) }}">formulaire</a> pour désactiver ce produit.</p>
{% endblock %}
